// Page Model
import 'package:app/models/OrderModel.dart';

class PageModel {
  final String title;
  final String message;
  final OrderModel order;

  PageModel({this.title, this.message, this.order});
}
